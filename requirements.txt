# requirements.txt — Gestor BIM (Instalações Elétricas + Estrutural)
# Fixar versões é essencial para reprodutibilidade científica.
# Instalar com: pip install -r requirements.txt

streamlit>=1.35.0,<2.0.0
ifcopenshell>=0.7.0
gspread>=6.0.0,<7.0.0
google-auth>=2.29.0
reportlab>=4.2.0,<5.0.0
qrcode[pil]>=7.4.2,<8.0.0
pandas>=2.2.0,<3.0.0
Pillow>=10.3.0
